# Working with SASS

Style your prototypes with SASS for maintainable and organized CSS.

## Setting Up SASS

(NATALIE EXPLAIN if any specific setup is required for SASS.)

## Using SASS Files

Create `.scss` files in (NATALIE EXPLAIN the correct directory for SASS files).

Example:

```scss
$primary-color: blue;

body {
    background-color: $primary-color;
}
```
